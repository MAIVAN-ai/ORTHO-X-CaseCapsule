# Suggested fourth commit

```text
feat: expose Case Capsule as FHIR/DICOMweb-aware MCP persistence service

- add FHIR reference adapter with version-aware resource provenance
- add DICOMweb Study adapter with QIDO-RS verification and WADO-RS references
- add model-neutral MCP server for Case Capsule operations
- expose evidence, assertion, classification, confirmation, surgery and outcome tools
- reuse commit-3 runtime through replaceable backend facade
- add stdio and Streamable HTTP deployment patterns
- add credential-free environment/config examples
- add service security and human-identity trust-boundary guidance
- add adapter and end-to-end service tests
```
